import React from 'react';
import MenuGrid from '../modules/menu/MenuGridContainer';

const MenuPage = () => (
  <div>
    <MenuGrid 
    // items={menu} 
    />
  </div>
);

export default MenuPage;
